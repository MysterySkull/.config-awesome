local awful = require('awful')
local gears = require('gears')

local keys = require('variables.keys')

local _M = {
   clientbuttons = require('bindings.client.buttons').clientbuttons,
   clientkeys = require('bindings.client.keys').clientkeys,
}

return _M
